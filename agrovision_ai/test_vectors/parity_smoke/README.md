# Local Parity Smoke Images

Place temporary real runtime-parity images in this folder. Supported formats
are JPG, JPEG, PNG, and WebP. Image files are intentionally ignored by Git.

Record provenance and expected use before testing. Smoke images check runtime
consistency only; they must not be used to claim disease accuracy.
